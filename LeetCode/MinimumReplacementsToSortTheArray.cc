#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    long long minimumReplacement(vector<int>& nums) {
        long long counter = 0;
        while(!is_sorted(nums.begin(), nums.end()))
        {
            if(nums)
        }
    }
};