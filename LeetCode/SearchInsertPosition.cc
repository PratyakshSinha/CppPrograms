#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        long low = 0;
        long high = nums.size();
        long mid;
        long result = nums.size();

        
    }
};